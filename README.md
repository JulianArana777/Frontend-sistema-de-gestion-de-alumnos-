Crear un archivo .env y poner la siguiente variable:
 VITE_API_URL=http://localhost:8080/api #URL que usa Axios para conectarse al backend

Comandos para ejecturar el proyecto: 

docker compose up -d #Levantar frontend
